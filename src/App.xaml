<Application x:Class="emMDee.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:emMDee">
    <Application.Resources>
        <!-- Theme color resources — swapped at runtime by App.ApplyChromeColors() -->
        <SolidColorBrush x:Key="ChromeBackground"    Color="#F5F5F5"/>
        <SolidColorBrush x:Key="ChromeBorderBrush"   Color="#DDDDDD"/>
        <SolidColorBrush x:Key="TabBackground"       Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SearchBarBackground" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="ChromeForeground"    Color="#1E1E1E"/>
        <SolidColorBrush x:Key="ContentBackground"   Color="#FFFFFF"/>
        <SolidColorBrush x:Key="ScrollTrackBrush"    Color="#F0F0F0"/>
        <SolidColorBrush x:Key="ScrollThumbBrush"    Color="#C0C0C0"/>
        <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#A0A0A0"/>

        <!-- Custom MenuItem ControlTemplate — replaces the Aero2 template entirely so dark mode has
             no stray white borders, icon-column glyph panels, or system-color references. -->
        <Style TargetType="MenuItem">
            <Setter Property="Background"                Value="{DynamicResource ChromeBackground}"/>
            <Setter Property="Foreground"                Value="{DynamicResource ChromeForeground}"/>
            <Setter Property="BorderBrush"               Value="Transparent"/>
            <Setter Property="BorderThickness"           Value="0"/>
            <Setter Property="Padding"                   Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment"   Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="Bg"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      VerticalAlignment="Center"/>
                                    <TextBlock x:Name="GestureText"
                                               Grid.Column="1"
                                               Text="{TemplateBinding InputGestureText}"
                                               Margin="20,0,4,0"
                                               VerticalAlignment="Center"
                                               Foreground="{DynamicResource ChromeForeground}"
                                               Opacity="0.55"/>
                                    <Path x:Name="Arrow"
                                          Grid.Column="2"
                                          Data="M0,0 L4,3.5 0,7Z"
                                          Fill="{DynamicResource ChromeForeground}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Right"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}">
                                <Border Background="{DynamicResource ChromeBackground}"
                                        BorderBrush="{DynamicResource ChromeBorderBrush}"
                                        BorderThickness="1"
                                        Padding="2">
                                    <ScrollViewer CanContentScroll="True" Style="{x:Null}">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                                        Grid.IsSharedSizeScope="True"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                                <Setter TargetName="GestureText" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                                <Setter Property="Padding" Value="8,5"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource TabBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Separator in menu: thin line in ChromeBorderBrush. -->
        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource ChromeBorderBrush}"/>
            <Setter Property="Height"     Value="1"/>
            <Setter Property="Margin"     Value="0,3"/>
        </Style>

        <!-- Scrollbar style — minimal track+thumb, responds to theme via DynamicResource -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False"/>
            <Setter Property="Stylus.IsFlicksEnabled"       Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Border Background="{DynamicResource ScrollTrackBrush}"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Opacity="0" Focusable="False" IsHitTestVisible="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Opacity="0" Focusable="False" IsHitTestVisible="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="ThumbVisual"
                                                        Background="{DynamicResource ScrollThumbBrush}"
                                                        CornerRadius="3"
                                                        Margin="2"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbVisual" Property="Background"
                                                                Value="{DynamicResource ScrollThumbHoverBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbVisual" Property="Background"
                                                                Value="{DynamicResource ScrollThumbHoverBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
